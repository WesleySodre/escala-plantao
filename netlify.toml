[build]
  command = "pnpm install --frozen-lockfile && pnpm run build"
  publish = "dist/public"
  functions = "client/netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
