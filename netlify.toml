[build]
  base = "client"
  command = "pnpm install --frozen-lockfile && pnpm run build"
  publish = "dist/public"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
